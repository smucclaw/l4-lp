Enter a L4 program below, then click the "Run Queries" button in the top bar to evaluate all queries.
When each query completes, a corresponding execution trace will be displayed in the "Query Results" section.
Note that the whole pipeline, from parsing and transpilation to evaluation and processing of traces, runs directly in the browser.